import { FormEvent, useState } from "react";
import { X } from "lucide-react";
import { Project, User } from "../types";
import OrganizationResourceDrawer from "./tenant/OrganizationResourceDrawer";
import ProjectMemberDrawer from "./tenant/ProjectMemberDrawer";
import ProjectRoleManagement from "./tenant/ProjectRoleManagement";
import TenantMemberManagement from "./tenant/TenantMemberManagement";
import TenantOrganizationList from "./tenant/TenantOrganizationList";
import TenantProjectList from "./tenant/TenantProjectList";

export interface OrganizationSummary { id: string; slug: string; name: string; status?: "ACTIVE" | "DISABLED"; version: number; createdAt?: string; auditLogs?: Array<{ actor: { id: string; displayName: string; username: string } | null }>; _count?: { members: number; projectSpaces: number } }
type Tab = "organizations" | "projects" | "roles" | "members";
type Editor = "create-organization" | "edit-organization" | "create-project" | "edit-project" | null;
type ResourceDrawer = { organization: OrganizationSummary; kind: "members" | "add-member" | "projects" } | null;
type ProjectDrawer = { project: Project; adding: boolean } | null;
interface Props { organization: OrganizationSummary; organizations: OrganizationSummary[]; project: Project; projects: Project[]; currentUser: User; onUpdateOrganization: (name: string, status?: "ACTIVE" | "DISABLED") => Promise<void>; onUpdateProject: (input: { name: string; description: string; status?: "ACTIVE" | "DISABLED" }) => Promise<void>; onSelectProject: (id: string) => void; onCreateProject: (input: { name: string; description: string }) => Promise<void>; onDeleteProject: (id: string) => Promise<void>; onSelectOrganization: (id: string) => void; onCreateOrganization: (name: string) => Promise<void>; onDeleteOrganization: (id: string) => Promise<void>; showToast: (message: string, type?: "success" | "error") => void }

export default function OrganizationSpaceSettings(props: Props) {
  const { organization, organizations, project, projects, currentUser, showToast } = props;
  const [tab, setTab] = useState<Tab>("organizations"); const [editor, setEditor] = useState<Editor>(null); const [drawer, setDrawer] = useState<ResourceDrawer>(null); const [projectDrawer, setProjectDrawer] = useState<ProjectDrawer>(null); const [saving, setSaving] = useState(false); const [organizationName, setOrganizationName] = useState(""); const [spaceDraft, setSpaceDraft] = useState({ name: "", description: "" });
  const tabs: Array<[Tab, string]> = [["organizations", "组织管理"], ["projects", "项目空间"], ["roles", "角色权限"], ["members", "成员管理"]];
  const openEditor = (kind: Editor, item?: OrganizationSummary | Project) => { setEditor(kind); if (kind?.includes("organization")) setOrganizationName(item && "slug" in item ? item.name : ""); else setSpaceDraft(item && "key" in item ? { name: item.name, description: item.description || "" } : { name: "", description: "" }); };
  const submit = async (event: FormEvent) => { event.preventDefault(); if (!editor) return; setSaving(true); try { if (editor === "create-organization") await props.onCreateOrganization(organizationName.trim()); if (editor === "edit-organization") await props.onUpdateOrganization(organizationName.trim()); if (editor === "create-project") await props.onCreateProject(spaceDraft); if (editor === "edit-project") await props.onUpdateProject({ name: spaceDraft.name.trim(), description: spaceDraft.description.trim() }); showToast(editor.startsWith("create") ? "创建成功" : "保存成功"); setEditor(null); } catch (error) { showToast(error instanceof Error ? error.message : "操作失败", "error"); } finally { setSaving(false); } };
  const deleteOrganization = async (item: OrganizationSummary) => { if (!window.confirm(`确认删除组织“${item.name}”？此操作不可撤销。`)) return; try { await props.onDeleteOrganization(item.id); showToast("组织已删除"); } catch (error) { showToast(error instanceof Error ? error.message : "删除失败", "error"); } };
  const deleteProject = async (item: Project) => { if (!window.confirm(`确认删除项目“${item.name}”？`)) return; try { await props.onDeleteProject(item.id); showToast("项目已删除"); } catch (error) { showToast(error instanceof Error ? error.message : "删除失败", "error"); } };
  return <div className="space-y-4"><nav className="flex flex-wrap gap-1 rounded-2xl bg-slate-100/80 p-1">{tabs.map(([id, label]) => <button key={id} onClick={() => setTab(id)} className={`rounded-xl px-5 py-2.5 text-xs font-black ${tab === id ? "bg-white text-indigo-700 shadow-sm" : "text-slate-500 hover:text-slate-800"}`}>{label}</button>)}</nav>
    {tab === "organizations" && <TenantOrganizationList rows={organizations} selectedId={organization.id} onCreate={() => openEditor("create-organization")} onMembers={(item) => { props.onSelectOrganization(item.id); setDrawer({ organization: item, kind: "members" }); }} onProjects={(item) => { props.onSelectOrganization(item.id); setDrawer({ organization: item, kind: "projects" }); }} onAddUser={(item) => { props.onSelectOrganization(item.id); setDrawer({ organization: item, kind: "add-member" }); }} onToggle={(item) => { props.onSelectOrganization(item.id); void props.onUpdateOrganization(item.name, item.status === "DISABLED" ? "ACTIVE" : "DISABLED"); }} onEdit={(item) => { props.onSelectOrganization(item.id); openEditor("edit-organization", item); }} onDelete={(item) => void deleteOrganization(item)}/>}
    {tab === "projects" && <TenantProjectList rows={projects} organizationName={organization.name} selectedId={project.id} onCreate={() => openEditor("create-project")} onMembers={(item) => setProjectDrawer({ project: item, adding: false })} onAddMember={(item) => setProjectDrawer({ project: item, adding: true })} onToggle={(item) => { props.onSelectProject(item.id); void props.onUpdateProject({ name: item.name, description: item.description, status: item.status === "DISABLED" ? "ACTIVE" : "DISABLED" }); }} onEdit={(item) => { props.onSelectProject(item.id); openEditor("edit-project", item); }} onDelete={(item) => void deleteProject(item)}/>}
    {tab === "roles" && <ProjectRoleManagement organizationId={organization.id} project={project} onAddUser={() => setDrawer({ organization, kind: "add-member" })}/>}
    {tab === "members" && <TenantMemberManagement organizationId={organization.id} currentUser={currentUser}/>} {drawer && <OrganizationResourceDrawer organization={drawer.organization} kind={drawer.kind} onClose={() => setDrawer(null)}/>} {projectDrawer && <ProjectMemberDrawer organizationId={organization.id} project={projectDrawer.project} adding={projectDrawer.adding} onClose={() => setProjectDrawer(null)}/>}
    {editor && <div className="fixed inset-0 z-[120] flex items-center justify-center bg-slate-900/40 p-4"><form onSubmit={submit} className="w-full max-w-xl overflow-hidden rounded-2xl bg-white shadow-2xl"><header className="flex justify-between px-6 py-4"><div><h3 className="font-black">{editor.startsWith("create") ? "新增" : "编辑"}{editor.includes("organization") ? "组织" : "项目空间"}</h3><p className="mt-1 text-xs text-slate-400">{editor.includes("organization") ? "组织 ID 由系统自动生成" : "项目 ID 与 Key 由系统自动生成"}</p></div><button type="button" onClick={() => setEditor(null)}><X className="h-4 w-4"/></button></header><div className="grid gap-4 bg-slate-50/60 p-6">{editor.includes("organization") ? <input autoFocus required minLength={2} value={organizationName} onChange={(event) => setOrganizationName(event.target.value)} placeholder="组织名称" className="rounded-xl bg-white px-3 py-2.5 text-xs outline-none shadow-sm"/> : <><input autoFocus required minLength={2} value={spaceDraft.name} onChange={(event) => setSpaceDraft({ ...spaceDraft, name: event.target.value })} placeholder="项目名称" className="rounded-xl bg-white px-3 py-2.5 text-xs outline-none shadow-sm"/><textarea value={spaceDraft.description} onChange={(event) => setSpaceDraft({ ...spaceDraft, description: event.target.value })} placeholder="项目描述" className="h-24 resize-none rounded-xl bg-white px-3 py-2.5 text-xs outline-none shadow-sm"/></>}</div><footer className="flex justify-end gap-2 px-6 py-4"><button type="button" onClick={() => setEditor(null)} className="rounded-xl bg-slate-100 px-4 py-2 text-xs">取消</button><button disabled={saving} className="rounded-xl bg-indigo-600 px-5 py-2 text-xs font-bold text-white">确认保存</button></footer></form></div>}
  </div>;
}
